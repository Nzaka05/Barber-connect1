const express = require('express'); const router = express.Router(); const messageController = require('../controllers/messageController'); const { ensureAuthenticated } = require('../middleware/auth'); router.use(ensureAuthenticated); router.get('/', messageController.getChatList); router.get('/chat/:id', messageController.getChat); router.post('/start', messageController.startConversation); module.exports = router;
